<% provide(:title, "#{@user.profile_name}") %>
<div class="page-header">
	<fieldset>
		<legend>
			<h1><%= @user.full_name %></h1>
		</legend>
		<div class="span">
          <label class="control-label">Current Profile Picture(for forum)</label>
	          <div class="thumbnail">
	            
	          </div>
        </div>
	</fieldset>	
</div>